# 新歌置顶和高亮展示功能

## 功能说明

本次更新增加了"新歌置顶和高亮展示"功能，允许管理员在后台配置是否将最近添加的新歌曲置顶展示，并使用特殊样式着重展示。

## 功能特性

### 1. 后台配置
- **启用开关**: 管理员可以在站点配置中开启/关闭新歌高亮功能
- **时间范围**: 可配置多少天内添加的歌曲算作"新歌"（1-30天，默认7天）
- **可视化配置**: 使用滑块组件直观配置天数

### 2. 前台展示
- **自动置顶**: 新歌会自动排在歌单顶部（按添加时间倒序）
- **视觉高亮**: 
  - 特殊背景色（天空蓝色调）
  - 发光边框效果
  - "NEW" 标签动画
  - 太阳图标脉动动画
  - 歌曲名使用主题色加粗显示
- **响应式适配**: PC端和移动端都有优化的展示效果

### 3. 排序逻辑
- **启用时**: 新歌优先 → 添加时间倒序 → 首字母 → 歌曲名
- **未启用时**: 首字母 → 歌曲名（原有逻辑）

## 修改文件清单

### 后端修改

#### 1. `/backend/db/init.js`
- 添加数据库字段 `highlight_new_songs` (是否启用)
- 添加数据库字段 `new_song_days` (新歌天数阈值)
- 自动迁移逻辑，兼容现有数据库

#### 2. `/backend/services/siteService.js`
- `getMeta()`: 返回新配置字段
- `updateSiteConfig()`: 支持更新新配置字段

#### 3. `/backend/services/playlistService.js`
- `getPlaylist()`: 
  - 读取站点配置，判断是否启用新歌高亮
  - 修改SQL排序逻辑，支持新歌置顶
  - 为每首歌曲添加 `isNewSong` 标记
  - 返回配置信息（前端可获知是否启用）

### 前端修改

#### 1. `/frontend/src/components/admin/SiteConfigPanel.jsx`
- 导入 `Switch` 和 `Slider` 组件
- 添加配置状态字段
- 新增配置UI区域：
  - 开关按钮（启用/禁用）
  - 滑块组件（选择天数）
  - 说明文字

#### 2. `/frontend/src/pages/HomePage.jsx`
- 导入 `Brightness7` 图标（新歌标识）
- 修改 `SongListItem` 组件：
  - **PC端**: 
    - 新歌背景和边框高亮
    - 发光阴影效果
    - 太阳图标 + "NEW" 标签
    - 歌名加粗和着色
  - **移动端**:
    - 更显眼的背景和边框
    - 更大的阴影效果
    - 动画标识

## 样式设计

### 颜色方案
- 使用主题色（天空蓝 `#6EC1E4`）作为新歌强调色
- 浅色模式：柔和的蓝色背景，透明度 8-18%
- 深色模式：更鲜艳的蓝色背景，透明度 15-30%

### 动画效果
1. **脉动动画**: 太阳图标透明度周期性变化（2秒周期）
2. **弹跳动画**: "NEW" 标签上下浮动（2秒周期）
3. **悬浮增强**: 鼠标悬停时，新歌有更显著的阴影效果

### 边框设计
- 新歌使用 2px 边框（普通歌曲 1px）
- 边框颜色与主题色一致，带透明度

## 配置步骤

### 1. 进入管理后台
登录后进入管理后台 → 站点配置

### 2. 找到"新歌展示配置"区域
在配置页面底部找到该区域

### 3. 启用功能
- 打开"启用新歌置顶和高亮显示"开关
- 调整"新歌天数"滑块（建议 7-15 天）
- 点击"保存配置"

### 4. 查看效果
返回首页，最近添加的歌曲将自动置顶并高亮显示

## 技术实现细节

### 后端逻辑
```javascript
// 判断是否为新歌（JavaScript 示例）
const now = new Date();
const newSongThreshold = new Date(now.getTime() - newSongDays * 24 * 60 * 60 * 1000);
const isNewSong = createdAt >= newSongThreshold;
```

### SQL 排序
```sql
ORDER BY 
  CASE 
    WHEN datetime(created_at) >= datetime('now', '-7 days') THEN 0 
    ELSE 1 
  END,
  created_at DESC,
  firstLetter, 
  songName
```

### 响应式判断
- PC端 (>=960px): 横向布局，图标和标签在歌名两侧
- 移动端 (<960px): 垂直布局，图标和标签与歌名在同一行

## 兼容性

- ✅ 向后兼容：不影响现有数据库和功能
- ✅ 自动迁移：启动时自动添加新字段
- ✅ 默认关闭：不影响现有用户体验
- ✅ 响应式：适配所有设备

## 性能考虑

- 时间计算在后端完成，前端只做展示
- 使用索引优化查询（created_at 字段）
- CSS动画使用 GPU 加速（transform）
- 不影响现有的筛选和搜索功能

## 未来扩展

可能的增强方向：
- [ ] 支持自定义"新歌"标签文字
- [ ] 支持自定义高亮颜色
- [ ] 新歌数量统计和显示
- [ ] 新歌专属筛选器
- [ ] 新歌通知功能

## 测试建议

1. **配置测试**:
   - 开启/关闭功能
   - 调整不同天数阈值
   - 保存并刷新页面验证

2. **展示测试**:
   - 添加新歌曲，验证是否置顶
   - 检查PC端和移动端样式
   - 测试深色/浅色模式

3. **性能测试**:
   - 大量歌曲时的加载速度
   - 筛选和搜索性能

4. **兼容测试**:
   - 已有数据库升级测试
   - 多浏览器兼容性测试

---

**开发时间**: 2026-01-31  
**版本**: v1.1.0  
**开发者**: FallSakura

